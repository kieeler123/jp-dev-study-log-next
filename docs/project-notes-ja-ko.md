
---

# ✅ docs/project-notes-ja-ko.md (너의 “메인 어필 문장 + Q&A”용)

README에는 길게 넣지 않고, 여기로 분리.  
아래 템플릿 쓰면 깔끔해.

```md
# Project Notes (JA/KR)

## Summary (JA)
既存のlocalStorageベースのCRUDログアプリを運用し、JSONでexportしたデータをNext.js(API Routes)+MongoDBに移行(import)して永続化しました。
開発中に500エラーやMongoDB SRV(DNS)問題もログベースで原因を特定して解決しました。

## Summary (KR)
기존 localStorage 기반 CRUD 로그 앱을 운영하다가 JSON으로 export한 데이터를 Next.js(API Routes)+MongoDB로 이관(import)하여 영구 저장 구조로 확장했습니다.
개발 과정에서 500 에러와 MongoDB SRV(DNS) 문제도 로그 기반으로 원인을 특정해 해결했습니다.

---

## Q&A (JA → KR)
(여기에 내가 만들어준 질문/답변을 넣으면 됨)
